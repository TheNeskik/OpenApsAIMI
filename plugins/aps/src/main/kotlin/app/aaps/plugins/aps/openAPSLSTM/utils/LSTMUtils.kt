package app.aaps.plugins.aps.openAPSLSTM.utils

object LSTMUtils {
    fun preprocessData(data: List<Double>): List<Double> {
        // Prétraitement des données pour le modèle LSTM
        return data
    }
}